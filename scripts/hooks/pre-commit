#!/bin/sh
# pre-commit hook -- delegates to the cross-platform Go implementation.
# Works on Linux, macOS, and Windows (via Git for Windows' bundled sh.exe).
#
# Install once with: make hooks
exec go run "$(git rev-parse --show-toplevel)/scripts/hooks/pre-commit.go"
